##########################################
#
# Test directives - TODO use bundler
#
##########################################

test:
  override:
    - gem install page-object && gem install watir-webdriver && rspec search_youtube_spec.rb