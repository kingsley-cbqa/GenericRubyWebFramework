##########################################
#
# Test directives - TODO use bundler
#
##########################################

test:
  override:
    - bundle install && bundle exec rake spec[functional,parallel] --trace